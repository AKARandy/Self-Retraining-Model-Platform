# Development-only deps. Production deps remain in requirements.txt.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
alembic>=1.13
psycopg[binary]
pytest>=8
pytest-cov>=5
httpx
ruff>=0.6
