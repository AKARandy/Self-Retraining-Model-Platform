[pytest]
addopts = -ra --strict-markers --strict-config
markers =
    unit: fast unit tests (no DB)
    integration: integration tests requiring disposable Postgres
    pipeline: pipeline helper tests
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
